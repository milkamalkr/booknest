 1  python
    2  sudo apt update
    3  sudo apt -y upgrade
    4  apt install python
    5  sudo apt install python
    6  sudo apt install python3
    7  python -v
    8  python 
    9  python3
   10  sudo apt install -y python3-pip
   11  sudo apt install -y python3-venv
   12  mkdir envirs
   13  cd envirs/
   14  python3 -m venv my_env
   15  source my_env/bin/activate
       pip install bcrypt<4.0.0 
   17  pip install fastapi
   18  git
   19  sudo apt install git-all
   20  cd 
   21  git clone https://github.com/milkamalkr/booknest.git
   22  cd booknest/
   23  ls
   24  cd fastapi-template/
   25  uvicorn main:app --reload
   26  pip install uvicorn
   27  uvicorn main:app --reload
   28  pip install pydantic[email
   29  pip install pydantic[email]
   30  uvicorn main:app --reload
   31  pip install jose
   32  uvicorn main:app --reload
   33  pip list
   34  pip uninstall jose
   35  pip install python-jose[cryptography]
   36  uvicorn main:app --reload
   37  pip install psycopg2-binary
   38  uvicorn main:app --reload
   39  ufw allow 8000
   40  uvicorn main:app --reload
   41  uvicorn main:app --host 0.0.0.0 --port 8000

   sudo apt-get install postgresql

id, title, author, owner_id, description, image_url, tags, published_year, created_at, current_renter_id, rent_per_week, status


  
